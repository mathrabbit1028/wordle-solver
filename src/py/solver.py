from query import query
# importing others in grader.py and query.py is not premitted

def wordle_solve(words) -> None:
    for word in words:
        res = query(word)
    # write logic of your algorithms
    # returing values is not premitted
    # calling input and output functions is not permitted
